import { DisciplineRegisteredStudents } from './discipline-registered-students.model';

describe('DisciplineRegisteredStudents', () => {
  it('should create an instance', () => {
    expect(new DisciplineRegisteredStudents()).toBeTruthy();
  });
});
