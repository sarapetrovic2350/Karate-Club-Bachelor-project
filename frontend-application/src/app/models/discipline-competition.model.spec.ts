import { DisciplineCompetition } from './discipline-competition.model';

describe('DisciplineCompetition', () => {
  it('should create an instance', () => {
    expect(new DisciplineCompetition()).toBeTruthy();
  });
});
